import { z } from "zod";
import { useAppForm } from "@/components/_form";

const schema = z.object({
  // name: z.string({ required_error: "Error message" })
});

type <%= formName %>Data = {
  // name: string;
};

export const use<%= formName %> = () => {
  const form = useAppForm({
    validators: {
      onSubmit: schema,
    },
    defaultValues: {
      // name: "",
    },
    onSubmit: ({ value }) => {}
  });

  return {
    form
  };
};
